import cv2 as CV
import numpy

# print("run in vscode")

def get_8x8_Block(x_position, y_position, source_array):
        return 0

frame1_handle = CV.imread('frames/f1.png')
cv2.imshow("raw", frame1_handle)
cv2.waitKey(0)